{
  "name": "6_node-chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || set \"NODE_ENV=test\" && mocha server/**/*.test.js  -t 3000",
    "test-watch": "nodemon --exec \"npm test\" "
  },
  "author": "nodemon --exec ''",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "moment": "^2.24.0",
    "node": "10.15.1",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "expect": "^24.5.0",
    "mocha": "^6.0.2"
  }
}
